#include <bits/stdc++.h>

#define PRINT(x) std::cout << x << std::endl;

int main(){
    FILE *file = fopen("../input.txt", "r");
    if(file == NULL){
        print("Error with reading file!");
        return 0;
    }

}